#include "main.h"
/**
 * main - check the code
 * _isupper - check upper
 * @c : parameter ofvd function
 * Return: return per instruction
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
