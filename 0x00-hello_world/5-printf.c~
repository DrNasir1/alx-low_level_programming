#include <stdio.h>
/**
 * main - the programme starts and ends with the main
 * return: always returns 0 whend success
 */
int main(void)
{
	(printf"with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
